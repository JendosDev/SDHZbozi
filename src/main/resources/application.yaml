spring:
  application:
    name: SDHZbozi

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:mysql://zephyr.proxy.rlwy.net:17133/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Prague
    username: root
    password: zqdWhmyCrctHFAzuHgFOfHPxBQXmkhwE
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: ${PORT:8081}

cloudinary:
  url: ${CLOUDINARY_URL:}
  folder: ${CLOUDINARY_FOLDER:sdh-zbozi}